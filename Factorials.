#include <stdio.h>
#include<conio.h>
void main(){
  int i,fact=1,num;
  clrscr();

  printf("Input the number : ");
  scanf("%d",&num);

  for(i=1;i<=num;i++)
      fact=fact*i;

  printf("The Factorial of %d is: %d\n",num,fact);
  getch()(;
}
